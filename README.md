# Chat App MVP (React Native + Node.js + Socket.IO)

This archive contains two folders:
- /server — Node.js + Express + Socket.IO + MongoDB
- /mobile — Expo React Native app

Server uses localhost MongoDB by default. Mobile is configured to talk to http://localhost:4000 (emulator).
See each folder README for details.
