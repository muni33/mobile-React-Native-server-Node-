# Server (Node.js + Express + Socket.IO)

## Setup
1. cd server
2. npm install
3. Copy .env.example -> .env and edit if needed
4. Run: npm run dev

## Notes
- Default MONGO_URI is mongodb://localhost:27017/chatapp in .env.example
- JWT_SECRET default used if env not provided (development only)
