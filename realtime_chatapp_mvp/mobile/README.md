# Mobile (Expo React Native)

## Setup
1. cd mobile
2. npm install
3. Start: npm run start
4. Make sure the server is running on localhost:4000 (for emulator). If testing on a physical device, update the API base URL in /src/api/api.js to your machine LAN IP.
